If base_msg_time is 0, bandwidth will be set based on the config.json value. Otherwise, bandwidth
will be set based on base_msg_time.

if sync, must specify num children


ps_return_threshold: float - seconds to wait after receiving first update from worker for more updates
to come in before sending back (async)


inbound/outbound_bw: bw in megabits


num_workers: must match num workers in nodes, just used for data sharding stuff